# ============================================================
# Greece Chauffeur Service — Environment Configuration
# ============================================================

# Cloudflare D1 Database binding is defined in wrangler.jsonc (DB)

# Optional Email Provider API Keys (Phase 4)
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM=dispatch@greecechauffeur.com

# Optional Cloudflare R2 Document Storage (Phase 6)
# R2 binding is defined in wrangler.jsonc (STORAGE)
